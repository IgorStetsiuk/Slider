
// let iterator = config.itemsPerPage;
// this.slides.map(item =>{
//    if(item.classList.contains('visible')) {
//        item.classList.remove('visible');
//        return item;
//    }
//  while (iterator!==0 && iterator-- ){
//        return item.classList.add('visible')
//  }
//
// });

// const arrayToFilter = config.currentVisibleSlides || this.slides;
//
//
// const nextBatch = arrayToFilter.filter(item => {
//     if (item.classList.contains('visible')) {
//         item.classList.remove('visible');
//         config.currentVisibleSlides = arrayToFilter.splice(1,1);
//
//         return;
//     }
//     return item;
//
//
// });
// this.makeActiveSlides(nextBatch);